models:
  Author:
    name: string
    job: string
    meta_descreption: string

  Slug:
    slug: string

  SlugTranslation:
    id: id
    title: string
  Tag:
    name: string

  Artical:
    title: string
    relationships:
      hasMany: Tag, Author
      hasOne: Slug
    short_descreption: string
    descreption: string
    meta_keyword: string
    tag_id: foreign
    author_id: foreign
